# 1.0.1 发布检查

2026-09-15 更新：名称与自动触发、必需依赖初始化、七种语言 README、产品截图与封面已纳入发布源。独立 Skill 和插件使用同一源文件，包内 Skill 版本为 1.0.1。离线校验与压缩包回读结果见对应发布目录的清单。

本次不重新创建线上行程，也不将仓库推送视为网页部署或实时机酒查询验收。以下 1.0.0 内容为历史记录，保留其原检查日期；历史配置阻塞不代表当前服务状态。

# 1.0.0 上线检查

本清单区分发布包准备与线上体验验收。检查日期：2026-09-08。

## 发布包

- [x] 原开发 Skill、旧插件源和两份安装副本已分别归档，并附逐文件 SHA-256 清单。
- [x] 请求默认指向 `https://journione.ai/api/skill-roadbook`，保留接口 schema 与技术调用名。
- [x] 用户措辞、安装、示例、隐私、依赖与排错说明已补齐。
- [x] 18 组离线契约测试通过，包含默认正式域名、错误域名拒绝、显式测试环境、同键重试、地图卡片和本地文档链接。
- [x] Skill Creator 与 Plugin Creator 结构校验通过；试组装插件内 41 个 Skill 文件与开发源一致。
- 正式压缩包的逐文件回读校验及包 SHA-256 见同目录 `.sha256.json` 清单。

## 正式环境

- 已用 HTTPS HEAD 访问正式域名首页，返回 200；这不等于生成接口或登录体验通过验收。
- Python 默认 HTTP 客户端 GET 首页及接口返回 Cloudflare 403（1010），需结合实际 Node.js 请求结果判断客户端访问情况。
- [x] 实际 Node.js HTTP 客户端可到达正式生成路由。
- **上线阻塞：** `GET https://journione.ai/api/skill-roadbook` 返回 HTTP **503**，JSON 为 `{"error":"AGENTAUTH_APP_KEY 未配置。","reason":"service_not_configured"}`。这是服务端配置名，不是要求旅行者提供的 Token；由站点维护者配置后复测。本任务未改云端配置。
- Node.js GET 首页返回 200；`/privacy` 与 `/terms` 返回 200 的前端 HTML 外壳，尚不能据此确认政策正文有效。
- Python 的 403 属于不同客户端的边缘访问结果，Node.js 已到达应用并取得上述 503；不把两者混为相同故障。
- [ ] 用无私人媒体的确认行程真实创建，得到同源 `/preview/<trip_id>`；同请求同键重放仍返回同一行程。
- [ ] 页面内容、Journal / Map、图片生成、逐日地图和点位交互通过实际验收。
- [ ] 免登录查看、登录保存、个人编辑持久化、分享与账号隔离通过实际验收。
- [ ] 网站正式条款与隐私政策内容已核验后，再将真实 URL 加入插件元数据。
- [ ] 酒店与航班的可选能力在目标客户端完成实测；TourMind 新版依赖另行验证。

以上为 2026-09-08 的本地发布包准备记录，当时未安装正式包、未推送仓库、未发布插件市场、未修改网站或云端配置。后续 GitHub 仓库推送仅分发 Skill 源文件，不代表上述线上流程已完成；未实际执行的线上流程不打勾。

## 维护者复核

在 Skill 根目录运行 `node scripts/test-poster-contract.mjs`，再用 Skill Creator 的 `quick_validate.py` 检查本目录；插件解压后用 Plugin Creator 的 `validate_plugin.py` 检查 `journione`。

上线时如接口返回旧域名，不替换链接继续交付。修正服务端公开链接配置，保留原请求与幂等键，再核验同一行程。网络问题不通过切回测试域名绕过。

## 授权与发布元数据

本包未替权利人选定开源许可证，也未虚构公开源仓库、客服邮箱或政策链接。若分发渠道要求这些字段，维护者应在发布到该渠道前补充已确认信息；文档准备不等于授予第三方开源许可。
